// TODO
//
// import { createSelector } from 'reselect';
//
// export const createLmapSelector = lmapId => (
//   createSelector(
//     state => state.get(['lmaps', lmapId]),
//     lmap => ({ lmap })
//   )
// );
