import React from 'react';
import PropList from './PropList';

const CentralPane = () => (
  <div>
    <h1 style={{ color: 'red' }}>WORK IN PROGRESS! Come back later.</h1>
    <PropList />
  </div>
);

export default CentralPane;
